{
  "rules": {
    ".read": true,
    "presence": {
      "$sessionId": {
        ".write": true,
        ".validate": "newData.hasChildren(['label', 'updatedAt', 'cursorVisible'])"
      }
    }
  }
}
